checkpointer 降级: 测试模式：强制内存检查点（无跨进程持久化）
═══ SSE 流式 + 合并意图 验收（FakeStreamLLM 复现 OpenAI 流式契约）═══
── 1. kb_qa 流式（标签解析 + 流式推送）──
  ✔ 事件序列含 ready/token/done
  ✔ token 流不含 <intent> 标签（已剥离）
  ✔ done.intent=kb_qa（标签被正确解析用于路由）
  ✔ token 拼接 == done.answer（前端零特判还原）
  ✔ 答案正文完整（不含标签残留）
    answer='根据《考勤管理制度》第3条，请假需提前向主管申请。'
── 2. chitchat 规则短路（零 LLM）仍走 SSE ──
  ✔ chitchat 也产出 done
  ✔ chitchat intent=chitchat（规则短路）
  ✔ chitchat 非 degraded

结果: 8 通过 / 0 失败
